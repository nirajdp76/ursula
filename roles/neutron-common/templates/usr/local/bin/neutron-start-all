#!/bin/bash

start {{ neutron.service_names[os].neutron_server }}
start {{ neutron.service_names[os].neutron_linuxbridge_agent }}
start {{ neutron.service_names[os].neutron_metadata_agent }}

# neutron-dhcp-agent and neutron-l3-agent will be started
# when neutron-linuxbridge-agent is started due to service deps
