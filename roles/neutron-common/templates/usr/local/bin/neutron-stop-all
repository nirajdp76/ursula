#!/bin/bash

stop {{ neutron.service_names[os].neutron_server }}
stop {{ neutron.service_names[os].neutron_linuxbridge_agent }}
stop {{ neutron.service_names[os].neutron_metadata_agent }}

# neutron-dhcp-agent and neutron-l3-agent will be stopped
# when neutron-linuxbridge-agent is stopped due to service deps
